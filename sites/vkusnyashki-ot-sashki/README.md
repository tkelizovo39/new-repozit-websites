# Вкусняшки от Сашки — лендинг

## Статус приёмки (2026-09-13)

Пройден весь пайплайн: content-writer → designer → developer → seo-specialist.
Сайт открыт и проверен целиком в браузере (headless Chrome) — все секции
рендерятся, конструктор заказа и корзина работают, ошибок в консоли нет.

По чек-листу `docs/site-checklist.md`:
- **Контент** — ✅ все обязательные разделы есть, тон по брифу, фактов сверх
  брифа/материалов заказчицы не добавлено.
- **Дизайн** — ✅ палитра и шрифты из style-guide применены везде.
- **Вёрстка** — ✅ открывается без ошибок, внутренние ссылки/якоря работают,
  адаптивность проверена (390–1280px), alt-тексты у изображений есть.
- **SEO** — ✅ title/description, sitemap.xml, robots.txt, Open Graph,
  structured data — готовы, плейсхолдер домена явно помечен TODO в коде и в
  `seo/seo-report.md`.
- **Общее** — ✅ всё внутри `sites/vkusnyashki-ot-sashki/`, другие сайты не
  затронуты.

### Открытые вопросы к заказчице (не блокируют публикацию)
- Цена на «Сырники классические» — пока «уточняется» (см. `brief.md`).
- Ссылки на Instagram и Telegram — аккаунты регистрируются, кнопки на сайте
  готовы, но неактивны (см. раздел про соцсети ниже).
- Часть фото — временные (в т.ч. два коллажа `sbor-produkcii-*.jpg`, фото
  упаковки, задвоенное фото для обоих видов пельменей) — сайт спроектирован
  так, что замена файла по имени не требует правок кода.
- Плейсхолдер домена `https://example.com/` нужно заменить на реальный после
  публикации (список мест — в `seo/seo-report.md`).

## Обновление 2026-09-13 (кнопки Instagram/Telegram — подготовлены заранее)

По brief.md (раздел про контакты, «РЕШЕНИЕ ИЗМЕНИЛОСЬ 2026-09-13»): заказчица
регистрирует аккаунты в Instagram и Telegram, но ссылок пока нет. Чтобы не
переверстывать сайт заново, когда ссылки появятся, сделано следующее:

- В спрайт `<svg style="display:none">` в `index.html` добавлены иконки
  `#icon-instagram` и `#icon-telegram` (тот же line-стиль stroke/viewBox
  24×24, что и у остальных иконок).
- Кнопки-ссылки на Instagram и Telegram добавлены в трёх местах: шапка
  (`site-header__actions`), футер (колонка «Контакты») и раздел «Контакты» —
  тем же классом `icon-link`, что и у WhatsApp, с новыми модификаторами
  `icon-link--instagram` / `icon-link--telegram` (фирменные цвета соцсетей,
  приглушённые заливкой 14%, чтобы не спорить с тёплой палитрой сайта —
  этих цветов нет в `design/style-guide.md`, т.к. сами соцсети появились в
  брифе уже после дизайн-этапа).
- Пока ссылок нет, кнопки видны, но неактивны: `href="#"`,
  `aria-disabled="true"`, приглушённый вид (`opacity:.45`, курсор
  `not-allowed`) и подсказка `title="Скоро"`/`aria-label="…— скоро"`. Клик
  по ним ничего не делает (гасится в `script.js`). Рядом в футере и в
  «Контактах» есть поясняющая подпись, что аккаунты оформляются.
- **Как включить, когда придут ссылки:** достаточно поменять всего 2
  константы в самом начале `script.js` — `INSTAGRAM_URL` и `TELEGRAM_URL`
  (сейчас `null`). Функция `setupSocialLinks()` при загрузке страницы сама
  подставит рабочий `href` во все три места, откроет ссылку в новой вкладке,
  уберёт `aria-disabled`/приглушённый стиль/подсказку «Скоро» и спрячет
  пояснительную подпись (когда заполнены обе ссылки). Разметку и CSS менять
  не нужно.

## Обновление 2026-09-13 (сырники «цена уточняется» + раздел «Как упаковываем»)

По новому контенту от content-writer (`content/syrniki.md`, `content/upakovka.md`,
обновлённый `content/sitemap.md`) добавлены два блока:

1. **Новая позиция меню «Сырники классические»** — отдельная категория меню
   в `script.js` (ключ `syrniki`, анкор `#syrniki`) с новым типом карточки
   `pack` (фикс. фасовка 10 шт, степпер считает количество упаковок) и общим
   флагом `priceTBD: true`, который можно навесить на любую позицию меню без
   известной цены:
   - на карточке вместо цены — «Цена уточняется» (класс
     `.dish-card__price--tbd`), «Итого по позиции» тоже показывает этот текст,
     а не «0 ₽»;
   - `computeCardTotal`/`lineTotal` для такой позиции всегда возвращают `0` —
     она не влияет на сумму заказа и прогресс до бесплатной доставки;
   - позицию можно добавить в корзину: она видна в панели заказа (с пометкой
     «Цена уточняется» вместо суммы) и попадает в текст заказа для
     WhatsApp/копирования отдельной строкой вида «Сырники классические — 2
     уп. по 10 шт (цена уточняется)», без знака "=" и без вклада в «Итого».
   - Когда заказчица пришлёт цену — достаточно добавить `rate`/`price` и
     убрать `priceTBD: true` у позиции в `script.js`, остальную логику
     менять не нужно.
   - Фото `assets/images/syrniki-klassicheskie.jpg` добавлено в список
     ожидаемых в `assets/images/README.md` (файла пока нет — работает
     плашка-заглушка).
2. **Новый раздел «Как упаковываем»** (`#upakovka`) — между «Доставка и
   оплата» и «Контакты» по обновлённому `content/sitemap.md`. Текст —
   дословно из `content/upakovka.md`. Под текстом — сетка из 4 слотов под
   фото/видео (`upakovka-1.jpg` … `upakovka-4.jpg`), не привязанных к
   конкретному блюду: заказчица будет заполнять их сама, с тем же
   механизмом `onerror` → плашка-заглушка, что и у остальных фото сайта.
   Пункт добавлен в навигацию шапки и в колонку «Информация» футера.

## Обновление 2026-09-12 (правки по ответам заказчика)

После ответов заказчика на открытые вопросы (см. `brief.md`, раздел
«Ответы заказчика...» и `decisions-log.md`, пп. 6–7) внесены две правки:

1. В разделы «О нас» и «Доставка и оплата» перенесён обновлённый текст
   контент-райтера — имя (Александра/Саша), опыт 5+ лет, абзац про сроки
   предзаказа (в наличии — в тот же день, под заказ — 1–2 дня из-за
   шоковой заморозки).
2. Все 4 позиции фрикаделек переведены с типа карточки `tiers` (два
   фиксированных веса) на новый тип `weight-anchor` — гибкий вес от 300 г
   с любым шагом, цена считается линейной интерполяцией между ценой «от
   300 г» и ценой «за 1 кг» из брифа. Подробности и статус допущения — в
   разделе «Решения по спорным/неоднозначным местам», п. 1 ниже.

## Стек

Обычный статический сайт без сборки: **HTML + CSS + vanilla JS**.
Никаких фреймворков, бандлеров, npm-зависимостей — выбран как самый
простой вариант для одностраничного лендинга небольшого локального
бизнеса (по правилам `.claude/agents/developer.md`: «если не уверен —
выбирай более простой вариант»).

- `index.html` — разметка страницы (шапка, hero, «о нас», меню/конструктор
  заказа, доставка и оплата, контакты, футер, панель заказа, шаблоны карточек).
- `styles.css` — все стили: палитра, типографика, layout, компоненты
  (кнопки, карточки блюд, сегмент-контролы, степперы, панель заказа) —
  по `design/style-guide.md`.
- `script.js` — данные меню (точные цены из `brief.md`), рендер карточек,
  логика конструктора заказа/корзины, сборка и отправка текста заказа в
  WhatsApp, копирование текста в буфер обмена.
- `assets/images/` — папка для фото блюд. Фото пока нет — сайт показывает
  плашку-заглушку (см. `assets/images/README.md` с полным списком ожидаемых
  имён файлов и инструкцией «просто положите файл с таким же именем»).

Шрифты Bitter / Nunito / Caveat подключены через Google Fonts (кириллица),
с веб-safe фолбэками из style-guide на случай недоступности CDN.

## Как открыть локально

Сборка не нужна — просто откройте `index.html` в браузере (двойной клик
или «Открыть с помощью» → браузер). Для корректной работы `fetch`/модулей
это не требуется, весь JS — обычные `<script>`, поэтому подойдёт и прямое
открытие файла (`file://`), и любой простой локальный сервер, если он уже
настроен в проекте (например `npx serve .` или расширение Live Server) —
но это не обязательно.

## Что реализовано (по пунктам ТЗ)

1. **Меню** — все 4 категории и все позиции из `brief.md` заведены в
   `script.js` (массив `MENU`) с точными названиями и ценами, плюс отдельная
   категория «Сырники» (добавлена 2026-09-13), разбиты на 5 вариантов
   карточек по типу товара: `tiers` (два фикс. веса), `unit` (за шт / за
   кг), `single` (одна цена, степпер веса), `weight-anchor` (гибкий вес,
   цена — линейная интерполяция, см. п. 1 ниже) и `pack` (фикс. фасовка,
   степпер считает упаковки; вместе с флагом `priceTBD: true` — для
   позиций без цены, см. «Обновление 2026-09-13» выше).
2. **Конструктор заказа / корзина** — добавление позиций, живой пересчёт
   суммы, прогресс-бар до бесплатной доставки от 5000 ₽, переключатель
   «Самовывоз / Доставка» со списком зон доставки и пометкой про 300–600 ₽
   за счёт покупателя.
3. **Кнопка «Отправить заказ в WhatsApp»** — `https://wa.me/79168232824?text=...`
   с URL-encoded составом заказа, суммой и способом получения.
4. **Кнопка «Скопировать заказ»** — копирует тот же текст в буфер обмена
   (`navigator.clipboard`, с запасным вариантом через `execCommand('copy')`
   для старых браузеров), с текстовым подтверждением.
5. **Ссылка на канал MAX** — `https://max.ru/channel_minimym_na_kuhne`,
   отдельными кнопками/ссылками в панели заказа, контактах и футере.
6. **«Позвонить и заказать»** (`tel:+79168232824`) — крупная кнопка в шапке
   и рядом с корзиной (в панели заказа), а также в разделе «Контакты».
7. **Доступность для пожилых** — крупный шрифт (16px база, крупные цены),
   кнопки не меньше 44px, никаких обязательных полей ввода (имя/телефон/
   адрес — явно помечены «необязательно», их отсутствие не блокирует
   отправку заказа).
8. **Точный адрес не публикуется** — везде только «СНТ Скоротово,
   Одинцовский район» с пометкой, что точный адрес высылается после
   подтверждения заказа.
9. **Плейсхолдеры фото** — см. `assets/images/README.md`: `<img>` уже
   ссылается на итоговые имена файлов, при отсутствии файла показывается
   плашка-заглушка через `onerror`; замена — просто положить файл с тем
   же именем, без правки кода.
10. **Адаптивность** — брейкпоинты 599 / 959 / 1279px из style-guide,
    сетка карточек `auto-fit minmax(260px,1fr)`, панель заказа —
    bottom sheet на мобильном, sticky-панель справа на десктопе.

## Решения по спорным/неоднозначным местам (важно для заказчика)

Бриф и дизайн-гайд в паре мест противоречат друг другу или не дают всех
данных. Ничего не придумано «от себя» по цифрам — ниже честно описано,
как это было решено, чтобы заказчик мог поправить при необходимости:

1. **Фрикадельки («продажа от 300 г») — ОБНОВЛЕНО 2026-09-12, подтверждено
   заказчиком.** Изначально было сделано как карточка типа «tiers» (выбор
   из двух фиксированных вариантов «от 300 г» / «1 кг»), т.к. в брифе для
   каждой позиции дано только две цифры. Заказчик подтвердил: продажа на
   самом деле идёт **любым весом от 300 г и выше** (300 г, 400 г, 1 кг и
   т.д.), а не выбором из двух фиксированных пачек. Точную формулу
   пересчёта цены между 300 г и 1 кг заказчик не назвал, поэтому применена
   **линейная интерполяция** между двумя точками из брифа: 300 г → цена
   «от 300 г», 1000 г → цена «за 1 кг» (после 1 кг тариф продолжается той
   же прямой). Это гарантирует, что ровно на 300 г и ровно на 1 кг цена
   совпадает с цифрами брифа, а между ними и выше — растёт плавно и
   пропорционально. Реализовано как новый тип карточки `weight-anchor` в
   `script.js` (степпер веса с шагом 100 г, как у типа `single`). Формула
   интерполяции — допущение разработчика (заказчик его не давал явно);
   если у заказчика есть другая логика пересчёта — можно скорректировать
   `computeWeightAnchorPrice()` в `script.js`, не трогая остальное меню.
2. **Перцы фаршированные и голубцы** — в брифе указана только одна цена
   (900 ₽/кг), без минимального объёма заказа. Реализован непрерывный
   степпер веса с шагом 0.5 кг и минимумом 0.5 кг (по аналогии с
   наименьшей продаваемой фасовкой у остальных весовых позиций меню) —
   это моё технические решение по умолчанию, минимум явно в брифе не
   указан. **Стоит уточнить**, есть ли у заказчика реальный минимальный
   вес заказа для этих двух блюд.
3. **Котлеты и отбивные («100 г/шт»)** — реализованы как вариант «за 1 шт
   / за 1 кг» (сегмент-переключатель), как буквально написано в заголовке
   раздела брифа «цена за 1 шт / 1 кг» и в style-guide (вариант B). Если
   считать, что котлета весит ровно 100 г, то цена «за 1 шт» и «за 1 кг»
   математически не бьются (10 котлет по факту не равны цене за 1 кг по
   тому же тарифу) — но это несоответствие уже есть в исходных данных
   брифа, а не внесено при вёрстке; цифры перенесены как есть.
4. **Фото-иллюстрации на каждую группу меню** (упомянуты как желательные
   в `content/menu-intro.md`) не добавлены отдельным блоком — фокус сделан
   на фото для hero, «О нас» и на фото в каждой карточке блюда (это
   явный, обязательный компонент из style-guide). Если нужны ещё и общие
   фото-баннеры на каждую категорию — это можно добавить отдельно.
5. **hero-hands** — необязательное фото из брифа добавлено как декоративный
   второй снимок в hero (маленькое фото поверх основного), а не как
   отдельный самостоятельный блок.
6. Раздел «Блины» из исходного меню заказчика **сознательно не включён**
   в сайт — как и указано в брифе (цены не подтверждены).

## Ограничения по задаче (не менялись)

- Тексты контент-райтера перенесены без изменения смысла (только разбиты
  на HTML-теги/абзацы).
- Палитра, шрифты и все компоненты — строго по `design/style-guide.md`
  (включая обязательное правило: тёмно-коричневый текст на зелёной
  WhatsApp-кнопке, а не белый, ради контраста).
- В `<head>` не добавлялись meta-теги, sitemap.xml, robots.txt,
  структурированные данные — это задача seo-specialist (папка `seo/`
  зарезервирована и не тронута).
